projection;
strict ( 2 );
use draft;

define behavior for Z_C_INC_ALGN //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action changeStatus;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;

  use association _Historia { create; with draft; }
}

define behavior for Z_C_INCT_H_ALGN //alias <alias_name>
{
  use update;
  use delete;

  use association _Incidente { with draft; }
}